[build]
command = "npm run build"
publish = "public"

[build.environment]
NODE_VERSION = "22"

# Exclude the problematic adapters directory from Netlify caching
[build.cache]
ignorePatterns = [".cache/adapters/**"]
