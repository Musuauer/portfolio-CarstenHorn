[build]
command = "npm run build"
publish = "public"

[build.environment]
NODE_VERSION = "22"

# Include .cache/adapters in function bundles since SSR engine needs it
[functions]
included_files = [
  ".cache/adapters/**",
  ".cache/adapters/**/node_modules/**",
  ".cache/functions/**"
]
